{
  "name": "doctrines-rust-documentation-tooling",
  "version": "0.1.0",
  "private": true,
  "description": "Reproducible Markdown formatting and linting for the Rust doctrine corpus.",
  "license": "MIT OR Apache-2.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check:markdown": "npm run check:markdown-format && npm run lint:markdown",
    "check:markdown-format": "prettier --check \"**/*.md\"",
    "format:markdown": "prettier --write \"**/*.md\"",
    "lint:markdown": "markdownlint-cli2 \"**/*.md\" \"#dist\" \"#node_modules\" \"#target\""
  },
  "devDependencies": {
    "markdownlint-cli2": "0.23.1",
    "prettier": "3.9.6"
  },
  "overrides": {
    "js-yaml": "5.2.2"
  }
}
